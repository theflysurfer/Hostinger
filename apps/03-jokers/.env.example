# Environment
NODE_ENV=production

# Server Port (must match Nginx proxy_pass configuration)
PORT=5000

# Neon PostgreSQL Database
# Format: postgresql://username:password@hostname/database?sslmode=require
# Obtenir depuis: https://console.neon.tech
DATABASE_URL=postgresql://user:password@ep-xxx-xxx.us-east-2.aws.neon.tech/jokers_prod?sslmode=require
